# Agent-side Python dependencies (no ROS2 needed).
# Install with: pip install -r requirements.txt

ollama>=0.3.0
jsonschema>=4.21
json-repair>=0.25
pyyaml>=6.0
streamlit>=1.30

# Test deps
pytest>=8.0

# tests/test_block_geometry.py and tests/test_image_decode.py import the
# detector, which imports cv2. Headless because nothing here opens a window,
# and the GUI build drags in system libraries that are absent on a bare CI box.
# Without it those two modules fail at *collection*, which aborts the whole run
# rather than skipping two files.
opencv-python-headless>=4.9
