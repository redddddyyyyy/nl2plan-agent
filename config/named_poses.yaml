# Named-pose lookup for the agent. Map frame of mobile_arm_sim's
# aws-small-house map; x and y in meters, theta in radians.
#
# One search pose per room, each ~0.7 m from where its block sits so the
# camera (range ~1.1 m) can confirm it. table is a standoff north of the
# drop table at (4.0, -2.5); place() drives the last stretch by touch.

home:
  x: 4.3
  y: -1.5
  theta: 3.1416

gym:
  x: -5.77
  y: -3.17
  theta: -2.36

bedroom:
  x: -6.9
  y: -0.1
  theta: 3.1416

bedroom_window:
  x: -4.5
  y: 1.4
  theta: 1.5708

lounge:
  x: 1.8
  y: 4.2
  theta: 0.0

table:
  x: 4.0
  y: -1.75
  theta: -1.5708
